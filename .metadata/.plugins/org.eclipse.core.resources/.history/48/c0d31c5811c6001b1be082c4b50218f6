package kodlamaio.hrms.dataAccess.abstracts;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

import kodlamaio.hrms.entities.concretes.EmployeeConfirm;
import kodlamaio.hrms.entities.concretes.Employer;

@Repository
public interface EmployeeConfirmDao extends JpaRepository<EmployeeConfirm, Integer>{

	
	//@Query("From EmployeeConfirm where employer.id=:employerId")
	//EmployeeConfirm getByEmployerId(int employerId);
	
	Employer getByEmployer(Employer employer);
	
	
}
